#include <iostream>
#include <vector>
#include <algorithm>
#include <map>

using namespace std;

#define int ll;
typedef long long ll;

void solve(){
    ll n, m;
    cin >> n >> m;
    std::vector<ll> arr(n);





}

signed main() {
    ll t;
    cin >> t;
    while(t--) solve();
    return 0;
}

